{
  "name": "halloween-game",
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "^3.0.3"
  },
  "scripts": {
    "format": "bun run prettier -w ."
  }
}
